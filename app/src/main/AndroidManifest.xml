<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.serkanozaydin.serkanozaydin">
    <uses-permission android:name="android.permission.INTERNET"/>
    android:hardwareAccelerated="true"

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo_last"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <service android:name="net.serkanozaydin.serkanozaydin.MyFirebaseMessagingService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        <service android:name="net.serkanozaydin.serkanozaydin.MyFirebaseInstanceIDService">
            <intent-filter>
                <action android:name="com.google.firebase.INSTANCE_ID_EVENT" />
            </intent-filter>
        </service>
        <activity
            android:name="net.serkanozaydin.serkanozaydin.SplashScreen"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="net.serkanozaydin.serkanozaydin.Gosterim_Sayfasi"
            android:theme="@style/AppTheme" />
    </application>

</manifest>